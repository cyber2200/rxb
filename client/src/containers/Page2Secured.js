import { connect } from 'react-redux'
import Page2Secured from '../components/secured/Page2Secured'

const mapStateToProps = state => ({})

const mapDispatchToProps = dispatch => ({
  dispatch
})

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(Page2Secured)
